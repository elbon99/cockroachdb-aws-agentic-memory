version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 24
        - npm ci
    build:
      commands:
        - npm run typecheck
        - npm test
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
